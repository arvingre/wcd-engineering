# wcd-engineering Memory

This directory contains durable, project-scoped memory for the WCD Engineering OS repository.

Start with `project-state.md`. Detailed decisions, incidents, lessons, patterns, and runbooks should be stored as separate records using the templates under `memory/templates/`.
